import { TextFieldWrapper } from "../ChannelSelector.styled";

export default function TextFieldView() {
    return (
        <TextFieldWrapper>
            <span>* 판매스토어를 선택해 구체 판매성과 데이터를 확인할 수 있으며, </span>
            <span>선택된 스토어들의 데이터를 비교할 수 있습니다. </span>
        </TextFieldWrapper>
    )
}